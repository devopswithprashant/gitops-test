apiVersion: v1
kind: Service
metadata:
  name: blog-service
  namespace: dwp
  labels:
    app: blog-backend
spec:
  selector:
    app: blog-backend
  ports:
    - protocol: TCP
      port: 9091
      targetPort: 9090
      name: http
  type: ClusterIP